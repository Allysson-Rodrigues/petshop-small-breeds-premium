{
  "name": "petshop-small-breeds-premium",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:all": "bash -lc 'npm run dev:backend & npm run dev:frontend & wait'"
  }
}
