{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Production-ready TypeScript + Express Foundations",
  "main": "dist/server.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "postinstall": "npm run prisma:generate",
    "dev": "tsx watch src/server.ts",
    "dev:seed": "bash -lc 'export ALLOW_DEMO_SEED=true; npm run prisma:generate && npm run prisma:push && npm run prisma:seed && tsx watch src/server.ts'",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/server.js",
    "start:test": "bash -lc 'export ALLOW_DEMO_SEED=true; npm run prisma:push && npm run prisma:seed && tsx src/server.ts'",
    "lint": "biome check src",
    "lint:fix": "biome check --write src",
    "format": "biome format --write src",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "prisma:push": "prisma db push --schema=./prisma/schema.prisma",
    "prisma:seed": "prisma db seed --schema=./prisma/schema.prisma"
  },
  "prisma": {
    "seed": "./node_modules/.bin/tsx prisma/seed.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.4.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.0.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "qs": "^6.14.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.3",
    "@types/bcrypt": "^6.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.13.4",
    "@types/qs": "^6.9.18",
    "@types/supertest": "^6.0.3",
    "prettier": "^3.5.1",
    "prisma": "^6.4.1",
    "supertest": "^7.1.4",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
